name = "clawguard-webhook"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# Secrets (set via `wrangler secret put`):
# STRIPE_WEBHOOK_SECRET
# RESEND_API_KEY

[[d1_databases]]
binding = "DB"
database_name = "clawguard-licenses"
database_id = "19aa17ee-de6b-46a5-9cae-aa3c6a286cce" # Run `npm run db:create` and paste the ID here
